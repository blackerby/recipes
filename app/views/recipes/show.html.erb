<h1><%= @recipe.name %></h1>

<h3>Ingredients:</h3>
<p><%= simple_format(@recipe.ingredients) %></p>
  
<h3>Instructions:</h3>
<p><%= simple_format(@recipe.instructions) %></p>

<h3>Source:</h3>
<p><%= link_to @recipe.source_url, @recipe.source_url, target: :_blank, rel: :noopener %></p>
<p>
  <%= link_to 'Edit', edit_recipe_path(@recipe) %>
  <%= link_to 'Delete', @recipe, method: :delete, data: {
    confirm: "Are you sure you want to delete #{@recipe.name}?"
  } %>
</p>